export const tokenService = {
  register,
  login,
};

function register(token) {
  // TODO: Add Axios fetch to server
  return token;
}

function login(token) {
  // TODO: Add Axios fetch to server
  return token;
}
