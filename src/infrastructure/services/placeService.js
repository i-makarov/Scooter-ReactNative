import {DataStore} from '@aws-amplify/datastore';


function activation(place) {
  // TODO: Add Axios fetch to server
  return place;
}
function deactivation(place) {
  // TODO: Add Axios fetch to server
  return place;
}
function booking(place) {
  // TODO: Add Axios fetch to server
  return place;
}
function unbook(place) {
  // TODO: Add Axios fetch to server
  return place;
}

export const placeService = {
  activation,
  deactivation,
  booking,
  unbook,
};